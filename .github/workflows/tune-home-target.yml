name: Tune UiDo Home Target
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  tune:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: Home preview is now fixed to the generated background."
