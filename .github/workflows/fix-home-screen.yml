name: Fix UiDo Home Screen
# Legacy one-shot patch. Keep available for manual recovery only.
on:
  workflow_dispatch:
jobs:
  fix:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: Home is now controlled by the approved preview commit."
